describe("Search", function() {
    const Search = require('../../routes/lib/search');

    //maybe we should have a different spec for each model?
    it("should search various models", function() {
        //Search.search();
    });
});